function numberToString(num) {
  return num.toString()
}

console.log(numberToString(456))

// parameter: number
// return: number converted to a string
// e.g. numberToString(456)
// results in '456'

// return num.toString()